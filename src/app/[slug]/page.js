// A panel's focused route. The scene (in the root layout) reads the URL slug
// and drives the focus animation, so this page renders no content itself.
export default function TitlePage() {
  return null;
}
